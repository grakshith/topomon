/**
 * Sigma.js Canvas Renderer Node Component
 * ========================================
 *
 * Function used by the canvas renderer to display a single node.
 * @module
 */
import { NodeDisplayData, PartialButFor } from "../../types";
export default function drawNode(context: CanvasRenderingContext2D, data: PartialButFor<NodeDisplayData, "x" | "y" | "size" | "color">): void;
